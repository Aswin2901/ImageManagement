{
    "builds": [{
        "src": "image_management/asgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "50mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "image_management/asgi.py"
        }
    ]
}